import { Module } from '@nestjs/common';

/**
 * NotificationModule (Phase 16)
 * BullMQ background worker integration for FCM and APNs push triggers.
 */
@Module({})
export class NotificationModule {}
