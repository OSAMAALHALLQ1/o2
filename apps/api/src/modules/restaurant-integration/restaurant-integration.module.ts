import { Module } from '@nestjs/common';

/**
 * RestaurantIntegrationModule (Phase 9 & 10)
 * Ingress for O2 branch POS webhooks, HMAC signature verification, Receipt QR redemption.
 */
@Module({})
export class RestaurantIntegrationModule {}
