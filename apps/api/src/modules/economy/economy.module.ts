import { Module } from '@nestjs/common';

/**
 * EconomyModule (Phase 4)
 * Handles append-only CurrencyLedger transactions, WalletBalance, Inventory & Shop.
 */
@Module({})
export class EconomyModule {}
