import { Module } from '@nestjs/common';

/**
 * CompanionModule (Phase 3)
 * Handles non-punitive time-decayed care state (Hunger, Cleanliness, Energy, Mood).
 */
@Module({})
export class CompanionModule {}
