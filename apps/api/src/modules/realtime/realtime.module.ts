import { Module } from '@nestjs/common';

/**
 * RealtimeModule (Phase 6)
 * WebSocket gateway with JWT authentication, Redis presence and heartbeat management.
 */
@Module({})
export class RealtimeModule {}
