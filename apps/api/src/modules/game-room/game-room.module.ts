import { Module } from '@nestjs/common';

/**
 * GameRoomModule (Phases 7 & 8)
 * Per-room sequential action actors executing deterministic @o2/game-core state machines.
 */
@Module({})
export class GameRoomModule {}
