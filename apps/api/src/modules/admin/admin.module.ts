import { Module } from '@nestjs/common';

/**
 * AdminModule (Phase 15)
 * RBAC guards, QR campaign management, physical voucher budget caps, audit logging.
 */
@Module({})
export class AdminModule {}
